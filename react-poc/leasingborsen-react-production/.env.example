# Supabase Configuration (Required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# AI Configuration (Optional - for hybrid PDF + AI extraction)
VITE_OPENAI_API_KEY=sk-your-openai-api-key

# AI Cost Management (Optional)
VITE_AI_MONTHLY_BUDGET=50      # Monthly budget in USD (default: 50)
VITE_AI_PER_PDF_LIMIT=0.25     # Maximum cost per PDF in USD (default: 0.25)

# Feature Flags (Optional)
VITE_ENABLE_AI_EXTRACTION=true  # Enable AI-powered extraction (default: true)
VITE_DEBUG_EXTRACTION=false     # Enable debug logging (default: false)

# Example Development Setup:
# 1. Copy this file to .env.local
# 2. Add your Supabase credentials
# 3. Optionally add OpenAI API key for AI extraction
# 4. System will work with pattern-only extraction if no AI key provided