# Testing environment (for Vitest)
# Currently uses mocks, but ready for actual test branch when available
VITE_SUPABASE_TEST_URL=http://localhost:54321
VITE_SUPABASE_TEST_ANON_KEY=test-anon-key
VITEST=true

# Feature flags for testing
VITE_AI_EXTRACTION_ENABLED=false
VITE_DEBUG_MODE=true